<h2>編集</h2>

<%= form_with model: @book, url: update_book_path(@book.id), method: :patch do |f| %>

    <h4>タイトル</h4>
    <%= f.text_field :title %>
    <p>本文</p>
    <%= f.text_area :body %>
    
    <%= f.submit '保存' %>

<% end %>